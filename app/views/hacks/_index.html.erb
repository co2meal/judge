<table class="table">
  <thead>
    <th>유저 이름</th>
    <th>상태</th>
    <th>시간</th>
  </thead>

  <tbody>
    <% hacks.each do |hack| %>
      <tr>
        <td> <%= link_to hack.user.email, hack.user %> </td>
        <td> <%= hack.status %> </td>
        <td> <%= created_at_with_hack(hack) %> </td>
      </tr>
    <% end %>
  </tbody>

</table>