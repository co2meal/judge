---

valid:
  code: |
    #include <iostream>
    
    using namespace std;
    
    int main() {
      int a, b;
      cin >> a >> b;
      cout << a + b << endl;
    }

mle:
  code: |
    #include <iostream>
    #include <string.h>

    using namespace std;

    char buffer[100*1024*1024*4]; // 400mb
    int main() {
      int a, b;
      cin >> a >> b;
      cout << a + b << endl;
    }

rte:
  code: |
    #include <iostream>

    int arr[2];

    using namespace std;

    int main() {
      // int a, b;
      // cout << a + b << endl;
      for(int i=0;i<2;i++) {
        cin >> arr[i+1];
      }
      int sum=0;
      for(int i=0;i<1000;i++) {
        sum += arr[i+1];
      }
      cout << sum << endl;
    }
two: {}
